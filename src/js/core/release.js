// Deliberately bump this pre-1.0 semantic version with each published release
// and its matching service-worker cache name. Use patch for fixes, minor for
// user-visible features, and reserve 1.0.0 for an explicit stability decision.
export const APP_RELEASE = "0.4.0";
