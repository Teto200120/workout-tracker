{
  "name": "hector-workout-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "http-server . -a 127.0.0.1 -p 4175 -c-1",
    "serve:lan": "http-server . -a 0.0.0.0 -p 4175",
    "lint": "eslint .",
    "format:check": "prettier --check package.json eslint.config.js playwright.config.js scripts/*.mjs src/js/catalog/*.js src/data/*.json tests .github README.md docs/*.md --end-of-line auto",
    "catalog:refresh": "node scripts/refresh-exercise-catalog.mjs",
    "catalog:validate": "node scripts/validate-exercise-catalog.mjs",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test \"tests/unit/*.test.js\"",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "check": "npm run lint && npm run format:check && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "~10.0.1",
    "@playwright/test": "~1.61.1",
    "eslint": "~10.6.0",
    "globals": "~17.7.0",
    "http-server": "~14.1.1",
    "prettier": "~3.9.4"
  }
}
